# Build output
/gage
/gage.exe
/dist/
# `go build ./...` run from inside cmd/gage drops the binary there, which
# the root-anchored patterns above do not cover.
/cmd/gage/gage
/cmd/gage/gage.exe

# Pinned lint toolchain, installed by `make lint` (see GOLANGCI_BIN)
/bin/

# Coverage profile, written by `make cover`
/coverage.out

# OS cruft
.DS_Store
Thumbs.db

# Editor
*.swp
