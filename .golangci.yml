version: "2"

linters:
  # "standard" is errcheck, govet, ineffassign, staticcheck, unused.
  default: standard
  enable:
    # Enforces the library/CLI split — see the forbidigo settings below.
    - forbidigo
    # A secrets manager should not be silently ignoring a write error or
    # a weak-permission file mode, so the security-oriented pass runs
    # from the start rather than being retrofitted once there is real
    # key material to get wrong.
    - gosec

  settings:
    forbidigo:
      # Resolve identifiers through type information rather than source
      # text, so `import osx "os"; osx.Exit(1)` is caught — that aliased
      # spelling is exactly the hole the hand-rolled checker this
      # replaces shipped with.
      #
      # Coverage below was established by probing this config against
      # deliberate violations, not assumed from the docs. What it
      # catches: every direct fmt.Print/Printf/Println; os.Exit and
      # os.Stdin/os.Stdout including under an import alias; and
      # fmt.Fprint* to os.Stderr.
      #
      # KNOWN GAP, deliberately accepted: forbidigo does not flag a
      # `fmt.Fprint*(w, ...)` call used as a bare expression statement
      # when w is anything other than os.Stderr — so a library-side
      #   fmt.Fprintln(os.Stdout, "x")
      # is NOT caught, though the same call in assignment position
      # (`_, _ = fmt.Fprintln(os.Stdout, ...)`) is. Also uncaught: an
      # aliased fmt print (`import format "fmt"; format.Printf(...)`).
      # Both are deliberate spellings rather than plausible slips, and
      # the accidental case this rule mainly defends against — a stray
      # fmt.Println left in a library function — is covered. Closing the
      # gap would mean a custom AST check, which this config exists to
      # replace; revisit if a real violation ever ships in that form.
      analyze-types: true
      forbid:
        - pattern: '^os\.Stdin$'
          msg: >-
            internal/gage must not read stdin; return a typed request and
            let cmd/gage own the terminal. See "Library architecture".
        - pattern: '^os\.Stdout$'
          msg: >-
            internal/gage must not write stdout; return a value and let
            cmd/gage render it. See "Library architecture".
        - pattern: '^os\.Exit$'
          msg: >-
            internal/gage must not exit the process; return an
            exitcode-carrying error and let cmd/gage map it.
        - pattern: '^fmt\.Print.*$'
          msg: >-
            internal/gage must not print; return a value or a typed
            request. See "Library architecture".
        # Not redundant with the os.Stdout rule: this is what actually
        # catches `fmt.Fprintln(os.Stdout, ...)` (see the note above).
        # The library returns values rather than writing to writers at
        # all, so it has no legitimate Fprint call to lose — non-test
        # library code uses only fmt.Errorf and fmt.Sprintf.
        - pattern: '^fmt\.Fprint.*$'
          msg: >-
            internal/gage must not write to a writer; return a value and
            let cmd/gage render it. See "Library architecture".
        # A different kind of purity rule from the four above: this one
        # isn't about the library staying terminal-agnostic, it's about a
        # security-sensitive test hook never reaching a real build. Its
        # message deliberately does *not* start with "internal/gage must
        # not" — that phrase is what the cmd/ exclusion below matches on
        # to exempt the other four rules there, and this one must stay
        # enforced in cmd/gage's non-test files, unlike those.
        - pattern: '^gage\.SetScryptWorkFactorForTests$'
          msg: >-
            SetScryptWorkFactorForTests must never be called outside a
            _test.go file. It exists only so a test binary can avoid
            gage's real, deliberately expensive KDF cost — see
            identityfile.go.
        # gage's second work factor, guarded identically. It is a separate
        # rule rather than one loosened pattern because the two hooks are
        # separate on purpose (D-ENROLL-SEAL-COST): merging them here
        # would be the first step toward merging the factors themselves.
        - pattern: '^gage\.SetEnrollmentWorkFactorForTests$'
          msg: >-
            SetEnrollmentWorkFactorForTests must never be called outside a
            _test.go file. It exists only so a test binary can avoid
            paying the sealing KDF cost per request — see enrollseal.go.

    errcheck:
      # Blank-identifier assignments (`_ = f.Close()`) are a deliberate,
      # visible "I considered this and chose to ignore it" and stay
      # allowed; a bare unchecked call does not.
      check-blank: false
      # Type assertions without the comma-ok form panic rather than
      # returning an error, which is not what this codebase wants.
      check-type-assertions: true

  exclusions:
    generated: lax
    rules:
      # The first five forbidigo rules above encode the *library layer's*
      # purity rule. cmd/gage is the CLI frontend — printing, reading
      # stdin, and exiting the process are its entire job. The `text`
      # filter is what keeps this from also exempting the
      # SetScryptWorkFactorForTests rule for cmd/'s non-test files: that
      # rule's message doesn't start with "internal/gage must not" (see
      # its own comment above), so it isn't matched here and stays
      # enforced everywhere outside a _test.go file, cmd/gage included —
      # a non-test cmd/gage file calling it would still ship a weakened
      # KDF in a real build, which is exactly the failure mode the other
      # five rules don't need to worry about.
      - path: ^cmd/
        text: 'internal/gage must not'
        linters:
          - forbidigo

      # scripts/ holds standalone dev-only tools (e.g. resetlocalstate)
      # that are never imported by internal/gage or shipped in the gage
      # binary — they're a second, `make`-invoked frontend in the same
      # sense cmd/gage is, so the same purity carve-out applies.
      - path: ^scripts/
        text: 'internal/gage must not'
        linters:
          - forbidigo

      # Test files are exempt from the purity rule by the same rule's own
      # terms ("outside _test.go files"): a library test legitimately
      # drives a fake terminal, vaultlock's subprocess harness has to read
      # stdin and exit to be a lock holder at all, and internal/gage's own
      # TestMain / cmd/gage's TestMain call SetScryptWorkFactorForTests to
      # lower the KDF cost for the whole suite (no `text` filter here —
      # every forbidigo rule, this one included, is legitimate from a
      # _test.go file).
      - path: _test\.go$
        linters:
          - forbidigo

      # Test files get a narrower gosec pass than shipped code: these
      # four codes describe test-fixture mechanics, not shipped
      # behavior, and would otherwise require the same suppression at
      # nearly every t.TempDir()-based test in the suite.
      #   - G304 (path traversal via variable): every fixture path is
      #     built from t.TempDir()/t.Name(), not attacker input.
      #   - G302/G306 (permissive/unusual file & dir modes): tests
      #     deliberately chmod fixtures to exercise permission-denied
      #     paths, or write with the same mode gage itself uses on disk.
      #   - G602 (slice index out of range): gosec's SSA-based bounds
      #     analyzer cannot always follow a `for i := range slice`
      #     invariant and flags provably in-bounds indexing (see
      #     memlock_test.go's TestAllocGivesEachBufferItsOwnPage).
      # G101 (hardcoded credentials), G204 (subprocess launched with a
      # variable) and G104 (unhandled errors) are deliberately NOT
      # listed here: each describes a mistake that's just as real in a
      # test file as in shipped code (a pasted real token, a
      # shell-injection-shaped subprocess call, a silently dropped
      # error), so those three are suppressed per call site with
      # `// #nosec <CODE> -- reason` instead, same as shipped code.
      - path: _test\.go$
        text: 'G304|G302|G306|G602'
        linters:
          - gosec

formatters:
  enable:
    - gofmt
